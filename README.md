Use the macro line `WoundCalculator.render()` to open the dialog. Fill in the Damge Roll and AP value. Upon clicking `calculate` it will run on an selected tokens and provide a chat card of the results
